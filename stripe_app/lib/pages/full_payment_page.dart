import 'package:flutter/material.dart';

class FullPaymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FullPaymentPage'),
      ),
    );
  }
}
